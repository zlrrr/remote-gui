# Binaries
remote-executor/dist/
gui/dist/

# TLS Certificates (production keys — keep dev certs in certs/ for convenience)
certs/*.key
# Keep .crt files for dev convenience (CA is already deleted after gen-certs.sh)

# Go build artifacts
*.test
*.out

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

server:
  host: "0.0.0.0"
  port: 8443

scripts:
  dir: "scripts"

records:
  dir: "records"

tls:
  ca_cert:     "certs/ca.crt"
  server_cert: "certs/executor.crt"
  server_key:  "certs/executor.key"
